using System;
using System.Collections.Generic;
using System.Text;

namespace InClass
{
    /// <summary>
    /// use this commenting structure for each method. two references for each sorting method
    /// 8 total methods
    /// read in a csv file as the input to your sorting methods, File.ReadLine to read one line of data at a time
    /// <see cref="url"/>
    /// /// <see cref="url"/>
    /// </summary>
    public static class SortingMethods
    {
        public static List<Guid> InsertionSort(List<Guid> theData)
        {
            return new List<Guid>();
        }
        public static List<Double> InsertionSort(List<Double> theData)
        {
            return new List<Double>();
        }
    }
}
